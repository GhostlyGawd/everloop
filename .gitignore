# Live run state (regenerated by ticks; templates are tracked, mutable copies are not)
state/counter.txt
.everloop-lock
*.log

# Secrets — never commit
.env
.env.*
!.env.example
secrets/

# Deps / OS
node_modules/
.DS_Store
Thumbs.db
