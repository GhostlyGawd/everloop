# GOAL

> The objective your everloop agent works toward, one bounded tick at a time. Replace this with
> your own goal. The `examples/claude` agent reads this each tick. Keep it concrete and check-able.

_Example:_ "Maintain and improve this repo: each tick, pick one small improvement, make it, and
note it in the ledger. Verify any pushed change against the remote before claiming it's done."
